dependencies {
    compile(
            libs.groovy,
            libs.guava,
            libs.slf4j,
    )

    testCompile(
            libs.spock
    )

    runtime(
            libs.logback
    )
}

apply from: rootDir.path + '/publish.gradle'