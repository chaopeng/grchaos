dependencies {
    compile(
            libs.groovy,
            libs.guava,
            libs.slf4j,
            libs.rxgroovy
    )

    testCompile(
            libs.spock
    )

    runtime(
            libs.logback
    )
}

apply from: rootDir.path + '/publish.gradle'